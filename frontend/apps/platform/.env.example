COINGECKO_API_KEY=api_key

# Mintlify docs at /docs (Host at /docs in Mintlify → use dashboard URL slug). Set in Vercel for production.
# MINTLIFY_SUBDOMAIN=your-subdomain

# Smart contracts: generate (OpenAI) + deploy (Stellar CLI) + interact (Stellar SDK)
# Deploy requires Stellar CLI (stellar): https://developers.stellar.org/docs/build/smart-contracts/getting-started/setup
OPENAI_API_KEY=your_openai_key
SOURCE_KEY=your_stellar_secret_key