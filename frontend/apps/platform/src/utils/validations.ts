/**
 * Centralized validation constants and regex patterns
 * Use these throughout the platform app for consistency
 */

// Password validation
export const PASSWORD_MIN_LENGTH = 8;
export const REGEX_NUMBER = /[0-9]/;
export const REGEX_SYMBOL = /[!@#$%^&*(),.?":{}|<>]/;

// OTP validation
export const OTP_LENGTH = 6;
export const REGEX_ONLY_DIGITS = /^\d+$/;
export const REGEX_SINGLE_DIGIT = /^\d$/;

