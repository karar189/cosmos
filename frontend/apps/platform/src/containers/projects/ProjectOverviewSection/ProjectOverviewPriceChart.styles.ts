import { flex, spacing } from '@core3/ui-components/styleSystem';
import { css } from '@emotion/react';

export const toggleContainer = css`
  ${flex.row}
  ${flex.align.center}
  ${flex.justify.between}
  ${spacing.gap.m}
`;
