import { cn } from "@/utils";

/** Frosted glass surface for marketing landing cards. */
export const glassCard = cn(
  "border-white/10 bg-white/[0.04] backdrop-blur-xl shadow-lg shadow-black/20",
  "transition-all duration-300 hover:border-white/20 hover:bg-white/[0.07]",
);
